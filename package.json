{
  "name": "thameelay-invoices",
  "version": "1.0.0",
  "private": true,
  "description": "Thameelay invoice intelligence app — smoke tests",
  "scripts": {
    "test": "playwright test",
    "test:smoke": "playwright test tests/smoke.spec.js --reporter=list",
    "test:headed": "playwright test tests/smoke.spec.js --headed"
  },
  "devDependencies": {
    "@playwright/test": "^1.50.0"
  }
}
